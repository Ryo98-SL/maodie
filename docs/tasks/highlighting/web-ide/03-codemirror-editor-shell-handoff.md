# 03 CodeMirror Editor Shell Handoff

状态：未开始。

## 完成摘要

待任务 03 完成后填写。

## 公共接口

待填写：

- Editor setup module path。
- Editor instance/read/update APIs。
- Example replacement and query source behavior。
- Run button source read behavior。

## 测试结果

待填写命令和结果。

## 已知限制

本任务只替换编辑器 shell，不渲染 highlight decorations，不显示实时 lexer diagnostics。

## 下一任务入口

任务 04 应从本任务确认的 editor setup 模块接入 worker session、decorations 和 diagnostics，不重写 editor lifecycle。

